# 马总量化回测平台 — 项目全景文档

> 最后更新: 2026-08-06 | 平台版本: 控制台 v3.x
> GitHub: https://github.com/mmyytt/quant-console
> Streamlit Cloud: https://quant-console-aslxkzbjvxeddzkygrvpf2.streamlit.app

---

## 1. 项目目标

### 想解决什么问题
- 为加密货币（ETH/BTC/SOL）构建一个**可回测、可实盘**的量化策略研发平台
- 支持三种策略范式：**经典单向（指标信号驱动）**、**Delta中性对冲（双腿锁仓+解封）**、**动能突破解封**
- 从数据获取 → 指标计算 → 策略回测 → 绩效分析 → AI诊断的全链路闭环

### 当前开发阶段
- **阶段定位**：回测引擎成熟，UI功能丰富，处于**持续优化与Bug修复**阶段
- 已部署到 Streamlit Cloud 公网访问
- 实盘交易模块（server.py + OKX API）已有框架但未深度测试

---

## 2. 技术架构

### 语言与依赖
```
Python 3.10 (主) / Python 3.14 (备)
Streamlit 1.60+  → UI
Plotly 6.0+      → 图表
Pandas 2.0+      → 数据处理
NumPy 1.24+      → 数值计算
Requests 2.28+   → API调用
PyArrow 10.0+    → Parquet读写
yFinance 0.2.30  → 数据兜底
```

### 文件结构与核心模块

| 文件 | 行数 | 职责 |
|:---|:---|:---|
| `app.py` | ~1700 | 主界面、指标Schema、策略组装、AI对话舱、预设管理 |
| `engine_core.py` | ~1300 | DataEngine、StrategyBase、BacktestEngineV2、PerformanceAnalyzer、MultiFactorRegime、PortfolioManager、对冲状态机 |
| `data_loader.py` | ~370 | 三重数据源轮询(OKX→Binance→CoinGecko→yfinance)、断层检测、强制重刷 |
| `fetch_data.py` | ~200 | 5m全量历史数据下载器 |
| `fix_july_gap.py` | ~100 | 数据断层修复脚本 |
| `ai_assistant.py` | ~160 | 多模型对话、Prompt注入、上下文构建 |
| `ai_analyst.py` | ~100 | DeepSeek API分析模块 |
| `server.py` | ~400 | 实盘HTTP服务+OKX API+引擎线程 |
| `dashboard.html` | ~250 | 独立Web看板（备用） |
| `requirements.txt` | 7 | 依赖清单 |

### 核心模块作用

| 模块 | 位置 | 功能 |
|:---|:---|:---|
| **DataEngine** | engine_core.py | 15min/5m数据加载、多周期重采样、完整性校验 |
| **MultiFactorRegime** | engine_core.py | EMA斜率+ADX+资金费率三维牛熊判定 |
| **StrategyBase** | engine_core.py | 策略基类，子类实现`generate_signals()` |
| **DynamicStrategy** | app.py | 从UI勾选的27种指标动态组装策略 |
| **BacktestEngineV2** | engine_core.py | 合约+轮动+动态仓位+对冲状态机 |
| **PerformanceAnalyzer** | engine_core.py | 收益率/夏普/回撤/胜率/盈亏比 |
| **PortfolioManager** | engine_core.py | 多Leg持仓、Delta对冲、分批建仓 |

---

## 3. 已完成功能

### 数据模块
- ✅ Binance历史月度Zip下载 (2017-2026)
- ✅ OKX/Binance/CoinGecko/yfinance 四重实时API
- ✅ 5m/15m/1h/4h/1D 全周期数据
- ✅ 断层自动检测+API补全 (`repair_gaps`)
- ✅ Parquet高性能存储
- ✅ 数据超过7天自动触发刷新

### 指标系统 (INDICATOR_SCHEMA, 27种)
- **趋势类**: EMA双均线、SMA三均线、SuperTrend、ADX/DMI、Ichimoku、Parabolic SAR
- **摆动类**: RSI、KDJ、MACD、CCI、StochRSI、Williams %R、Awesome Oscillator
- **通道**: 布林带、Keltner、Donchian、斐波那契回调
- **成交量**: OBV、VWAP、MFI、CMF、成交量突破
- **K线形态**: 锤头/倒锤、吞没、晨星/黄昏星、三连兵、Doji、Pinbar

### 策略系统
- ✅ **经典单向**: 27种指标自由组合 + AND/OR/加权打分 + 金字塔加仓
- ✅ **Delta中性对冲**: SPOT_LONG + FUTURES_SHORT 双腿锁仓、循环重开
- ✅ **动能突破解封**: 突破条件→平空解锁→裸多风控
- ✅ 牛熊方向过滤（牛市禁空/熊市禁多）
- ✅ 交易模式（双向/仅多/仅空）

### 回测引擎
- ✅ 严格无未来函数（信号用shift(1)，成交用下一根open）
- ✅ 3x杠杆永续合约 & 1x现货
- ✅ Fixed Capital / Fixed Risk 两种仓位模式
- ✅ ATR动态止损 + 移动止损(Trailing Stop)
- ✅ 爆仓检测 + 破产熔断(equity≤0)
- ✅ 资金费率每8h结算模拟
- ✅ 滑点入成交价 + 手续费按名义本金
- ✅ 断路器(连亏锁仓) + 冷却期
- ✅ OOS样本外验证(年化对比)

### 风控模块
- ✅ 单笔止盈%/止损%
- ✅ 动态仓位(牛市100%/震荡50%/熊市30%)
- ✅ 单笔风险占比(Fixed Risk模式)
- ✅ 组合回撤保护(3%强行全平)
- ✅ 连亏锁仓(笔数+天数)

### AI模块
- ✅ DeepSeek/OpenAI/Claude/Gemini 四模型统一API
- ✅ 翔哥AI对话舱(实时上下文+快捷提问)
- ✅ 一键策略诊断报告
- ✅ 交易心法System Prompt注入

### 其他
- ✅ 登录鉴权
- ✅ 策略预设保存/加载/删除
- ✅ 月度热力图+年度筛选
- ✅ K线图WebGL渲染+缩放按钮
- ✅ 手机端响应式CSS
- ✅ 配置文件导出
- ✅ 死机/掉线Telegram报警(server.py)
- ✅ OCO交易所端止盈止损挂单(server.py)

---

## 4. 当前最佳策略

### ETH 4H 经典单向 + 金字塔加仓

```
标的: ETH
周期: 4H
杠杆: 3x
仓位模式: Fixed Capital
单笔建仓: 30%

指标组合: EMA双均线(7/21) + RSI(14,30/70) + 成交量突破(20,1.5x)
信号逻辑: AND (全部满足)

止盈: 10% (保证金)
止损: 5% (保证金)

加仓管理:
  启用加仓: YES
  触发涨幅: 2%
  加仓比例: 50%
  最大次数: 3
  保本止损: YES

牛熊过滤: 开启 (ADX阈值25, 牛市阈值0.30)

历史回测 (2020-2026):
  总收益: +400%+
  年化: ~100-112%
  胜率: 38-42%
  最大回撤: 35-50%
```

---

## 5. 已发现的问题

### 保证金计算漏洞
- **已修复**：平仓时 `self.equity += margin + pnl` 重复加回从未扣除的保证金 → 改为只加 `pnl`
- **已修复**：对冲腿建仓时 `short_notional = margin * leverage` 导致空头名义价值 ≠ 现货 → 改为 `short_notional = spot_notional`

### 止盈止损真实性问题
- **已修复**：同Bar双触发(TP+SL)用bar方向判断 → 改为保守Worst-case优先取SL
- **已修复**：滑点和手续费混算(`TAKER_FEE + SLIPPAGE`) → 分离: 滑点入价、手续费按名义本金

### 最大回撤被低估
- 权益曲线之前用未闭合close价计算 → **已修复**为用open价
- 对冲腿浮动盈亏之前不计入权益曲线 → **已修复**

### 过拟合风险
- OOS评估之前用总收益直接减法(训练400% vs 测试97% = 误判-302%) → **已修复**为年化对比
- 训练集/测试集初始资金相同($10,000各自独立) → 正确隔离

### 数据源稳定性
- Binance API在Streamlit Cloud IP被拦截 → 改用OKX优先 + CoinGecko + yfinance三重轮询
- 月度Zip有延迟(~30天) → 自动检测数据>7天过期触发API增量补全

### 其他已知问题
- Python 3.14 SSL在Windows上不可用 → 必须用Python 3.10
- 端口8765/8766被僵尸进程占用 → 改用9000
- Clash代理劫持localhost → 需设NO_PROXY
- Pandas 2.1+ `applymap`弃用 → 改`map()`降级

---

## 6. 下一阶段开发计划

1. **实盘对接完善**：server.py + OKX API的OCO挂单、心跳守护已实现，需实盘测试
2. **多币种轮动回测**：BacktestEngineV2已支持多币种输入，但UI只有单币种
3. **金字塔加仓完善**：`_check_pyramiding`逻辑已实现在引擎中，UI参数已传递
4. **资金费率套利**：已有费率和Delta对冲基础，可扩展
5. **微信/钉钉实盘通知**：server.py已有Telegram Webhook，需扩展
6. **WebSocket实时行情推送**：提升UI实时性

---

## 7. 所有重要设计决策

| 决策 | 原因 | 影响 |
|:---|:---|:---|
| 无未来函数原则 (shift(1)+open价撮合) | 审计要求，保证回测真实性 | 收益率低于"偷看"版本 |
| 对冲腿不入self.positions | 防止`_check_positions`重复触发同Bar死循环 | 对冲权益需`_calc_total_equity`单独计算 |
| Sidebar全部包在st.form | 拖动滑块不触发重渲染 | 必须点提交才计算 |
| Parquet不用Pickle | Python 3.14/3.10 pickle不兼容 | 跨版本部署必需 |
| Schema-Driven UI | 加指标只需改INDICATOR_SCHEMA | UI和引擎自动适配 |
| 四模型统一API (_call_unified_api) | DeepSeek/OpenAI/Claude/Gemini不同格式 | 一个函数适配所有 |
| yfinance作为最后兜底 | Binance/OKX可能被Cloud IP拦截 | 牺牲成交量精度换稳定性 |

---

## 8. 开发过程中踩过的坑

1. **Python 3.14 SSL完全不可用** (Windows schannel EOF) → 换3.10
2. **端口8765/8766被9个僵尸进程占用** (keep_alive.bat无限重启) → 换端口9000，但cloud用8501
3. **Clash代理劫持localhost** (curl连127.0.0.1返回502) → 需`--noproxy`或`NO_PROXY=localhost`
4. **Parquet DatetimeIndex在3.10/3.14不兼容** → reset_index存储，load时重建
5. **Streamlit st.form内变量在UI渲染前未定义** (NameError) → form外声明默认值
6. **DynamicStrategy遍历selected_indicators遇到bool/list元数据键** (AttributeError) → 加`isinstance(cfg, dict)`守卫
7. **`_open()` verbose打印引用未定义的contracts变量** → 显式计算
8. **对冲Open→Close循环: 每Bar开平一次(22000笔)** → 对冲腿脱钩positions + `_last_closed_bar`防重开
9. **OOS用总收益对比而非年化** → 训练2年vs测试1年导致误判-302%
10. **pandas 2.1+ `applymap`改名`map`** → 兼容检测

---

## 9. 翔哥下一次接手时必须知道的信息

### 启动命令
```bash
cd "C:\Users\myt\Desktop\量化交易"
python -m streamlit run app.py
# 或双击桌面: run_app.bat
# Cloud: https://quant-console-aslxkzbjvxeddzkygrvpf2.streamlit.app
```

### 两个API账户
```
小账户 (~13U): key=480de720-3791...  pass=Wsmym1018.
大账户 (~940U): key=ad065771-187...  pass=Ma124600.
```

### 关键变量命名约定
- `strat_mode_key`: "classic" / "hedging" / "unlocking"
- `is_dual_leg`: True = 对冲或解锁模式
- `selected_indicators`: dict, key=指标名, value={"enabled":bool, "params":{schema_key:value}}
- 元数据键(非指标): `_weighted`, `_resonance_factors`, `_trade_mode`, `_pos_mode`

### 数据目录优先级
1. `C:\Users\myt\Desktop\eth_all\` (本地Windows)
2. `项目/data/` (云端/备份)
→ `_find_data_dir()` 决定

### 快速修复数据断层
```bash
python fix_july_gap.py        # 修复特定月份
python fetch_data.py --force  # 强制全量重下
```

### Git推送失败时
```bash
# SSL错误 → 重试
sleep 3 && git push origin main
```

### 重要：对冲模块与经典单向必须100%隔离
- 对冲腿不加入 `self.positions`
- 对冲状态机 `_hedge_state_machine` 独立管理
- 修改对冲逻辑时绝不触碰 `_try_rotate_entry` / `_check_positions` 的主路径
