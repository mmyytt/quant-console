# AGENTS.md

## 项目概述

量化回测 + 策略优化 + Live Trading 平台（QuantCode）。

## 开发原则

1. **安全优先**：交易核心代码永远优先于普通功能。
2. **简单优先**：不过度架构、不重复造轮子。
3. **保持可维护**。

## 禁止

- 过度架构。
- 重复造轮子。
- 破坏已有功能。
- 未验证修改交易逻辑。
- 自动简化或重构以下核心交易模块：`engine_core.py`、风控模块、仓位计算模块、交易执行模块、OKX API 接口模块。

## 交易系统最高优先级规则

交易核心代码永远优先级高于普通功能。

任何涉及资金、仓位、订单、杠杆、风险控制的修改，必须：

1. 先分析。
2. 再修改。
3. 再测试。
4. 最后提交。

禁止为了快速完成任务牺牲交易安全。

## 环境

- Python 3.10: `C:\Users\myt\AppData\Local\Programs\Python\Python310\python.exe`
- OKX 凭证仅存 `.env`（gitignored），禁止写入代码或提交 GitHub。

## Live Trading 安全（Phase 门禁）

未来接入 OKX / 模拟盘 / 小资金实盘 / 自动交易。任何新增下单/平仓/加仓/减仓/杠杆/TP/SL/仓位功能，必须先经过：Phase 1 数据读取测试（API/余额/持仓读取正常）→ Phase 2 模拟交易测试（信号/订单/资金计算正确）→ Phase 3 小资金实盘（人工确认开启，默认关闭自动交易）。

## 核心资产保护

禁止自动重构：`engine_core.py`、风控、仓位计算、杠杆计算、TP/SL、ATR、开平仓、OKX API 执行。修改前说明原因、影响范围、对收益/回撤/交易次数/风险的影响；修改后必须代码测试 + 历史回测 + 样本外验证 + 风险检查。

## 量化策略审计（quant audit workflow）

策略调整不单独回答，一次输出完整审计报告：策略逻辑、数据安全（未来函数/泄露）、过拟合、风险指标（回撤/Sharpe/Calmar/胜率/盈亏比/最大连亏）、实盘适配。

## 架构风险审计

新增大型模块（Live Trading / AI 分析 / 用户系统 / 交易执行）前，先输出 Architecture Risk Report（架构影响、模块风险、数据流、安全、性能、扩展），再开发。

## 工作模式与提交

- High Efficiency：复杂任务单次响应完成（理解 → 影响范围 → 方案 → 执行 → 测试 → 总结），禁止重复解释与过度架构。
- 提交：git diff → commit → push，输出修改文件、commit hash、测试结果。

## 交易系统修改等级（Trading System Change Classification）

### Level 1 低风险修改

包括：UI、页面展示、i18n、文案、报告格式。

允许直接修改。修改后运行基础测试。

### Level 2 中风险修改

包括：指标计算、参数优化模块、回测展示、数据分析。

要求：

- 修改前：说明影响范围。
- 修改后：回测验证、检查收益变化、检查回撤变化。

### Level 3 高风险修改

包括：`engine_core.py`、仓位计算、风险控制、杠杆、TP/SL、ATR、开仓逻辑、平仓逻辑。

要求：禁止直接修改。必须：

1. 创建备份。
2. 说明修改原因。
3. 说明可能影响。
4. 运行历史回测。
5. 运行样本外测试。
6. 检查风险指标。

### Level 4 生产风险修改

包括：OKX API、Live Trading、自动交易、真实订单。

要求：必须经过：回测 → 模拟盘 → 小资金测试 → 人工确认。

禁止直接开启自动交易。
